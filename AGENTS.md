# Ts 仓库开发约束

## 复用优先

开发任何新功能前，不论是绘图、统计检验、模型估计、数据处理还是评价指标，都必须先检索 Ts 包现有的公共函数、基类和模块，并优先通过继承、组合或扩展现有公共接口完成。

执行顺序：

1. 检索相关子包的公共 API、README、基类和测试，确认现有能力及契约。
2. 优先直接调用现有公共函数；其次为现有接口增加向后兼容的通用参数；再次才提取共享实现。
3. 只有现有能力无法满足已明确的公共契约时，才新增函数、类或模块。不得复制已有算法、绘图样式、验证逻辑或结果容器形成平行实现。
4. 新增实现必须在计划或变更说明中记录：检索过哪些现有能力、为什么不能直接复用、最终复用了哪些模块。
5. 测试必须同时覆盖新行为和被复用接口的兼容性；相关 README、公共 API 文档和示例必须同步更新。

跨子包功能应保持现有职责边界：

- `TsPlots`：统一绘图函数、布局与样式契约。
- `TsTests`：统计检验、检验结果和统计推断。
- `TsModels`：模型估计与结果接口，通过组合 `TsPlots` 和 `TsTests` 提供诊断能力。
- `TsUtils`：通用数据验证、转换和预处理。
- `TsMetrics`：评价指标。
- `TsSims`：数据生成与模拟。

若需要突破这些边界或引入重复实现，必须先说明原因并取得用户确认。
